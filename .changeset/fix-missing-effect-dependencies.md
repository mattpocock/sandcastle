---
"@ai-hero/sandcastle": patch
---

Declare missing `@effect/*` transitive peer dependencies (`@effect/cluster`, `@effect/experimental`, `@effect/rpc`, `@effect/sql`, `@effect/typeclass`) as direct dependencies to prevent peer dependency warnings and resolution issues.
