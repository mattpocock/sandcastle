---
"@ai-hero/sandcastle": patch
---

Add `--sandbox-provider`, `--backlog-manager`, `--create-label`/`--no-create-label`, and `--build-image`/`--no-build-image` flags to `sandcastle init` so the command can run end-to-end without interactive prompts.
